import React, { useState, useEffect } from 'react';

const InfoPage = ({ currentAccount, connectWallet, ...props }) => {
  return (
    <div style={{ height: '100%', color: 'white' }}>
      Info Page Show info on all chains here Number of NFTs minted and last minted in a table
    </div>
  );
};

export default InfoPage;
